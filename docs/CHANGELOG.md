# Версии

Теги git `vX.Y`. Внутри версии — что вошло и на какие грабли наступили.

## v0.4 — движки и «лишайник»
- Сменные движки: `life_cube/engines/` с реестром и интерфейсом `Rules`;
  «экология» обёрнута, «лишайник» — новый. Выбор из панели, `--engine`, `Engine(rules=)`.
- Движок «лишайник»: дождь и сток, эрозия камня в почву, смыв почвы в низины,
  стресс-мутации, динамические виды с генеалогией (до 64 одновременно).
- Кнопки «Перезапуск», «Случайные параметры», «Случайные + перезапуск».
- Палитра и маска видов на 256 значений в шейдере; таблица живых видов; имена и
  цвета динамических видов приходят каждый кадр.
- Баг: ткань растений без связи с землёй (кроны после гибели ствола) висела в
  воздухе — теперь гибнет (`require_substrate`).
- Папка `docs/`: индекс, архитектура, по документу на движок, этот файл.

## v0.3 — трофическая цепь и конструктор
- Геном 14 генов; травоядные и хищники ходят (чутьё, охота, тяжесть, энергия,
  деление, старость); камень на треть куба; панель конструктора существ и мира.
- Починка зависания на большом мире: команды в отдельном потоке, снимки по одному,
  предел размера куба, прореживание кадра, лог сервиса.

## v0.2 — веб-рендер и звук
- Engine/Snapshot; веб-просмотр (aiohttp + WebGL2): пауза/шаг/скорость, проекции,
  срезы, фильтры, организмы с устойчивыми id.
- Звук как сменный модуль: `sound/features` (Snapshot → SoundFrame), Web Audio в
  клиенте, оффлайн WAV.
- Ветвление растений и кроны; хищники первой версии (сидячие) — позже заменены.
- Скорость: боковое течение по всему объёму (13×), снимки отвязаны от симуляции.

## v0.1 — пакет из монолита
- `cube_ecology.py` разбит на модули, 14 тестов, GPU через CuPy, деплой на Mergel.
